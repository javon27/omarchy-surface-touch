-- Add whichever of these you want to your own ~/.config/hypr/bindings.lua.
-- Not applied automatically -- that file is yours to curate.

-- Toggle the wvkbd on-screen keyboard (see ../osk/).
o.bind("SUPER + SHIFT + K", "Toggle on-screen keyboard", "omarchy-toggle-osk")

-- Toggle the virtual trackpad panel (see ../trackpad/).
o.bind("SUPER + SHIFT + T", "Toggle virtual trackpad", "omarchy-toggle-trackpad")

-- Lock/unlock auto-rotate to the current orientation (see ../auto-rotate/).
o.bind("SUPER + SHIFT + R", "Toggle rotation lock", "omarchy-toggle-rotation-lock")
